#include <iostream>
#include <vector>
#include <cmath>

using namespace std;
const int MAX = 50001;

int main() {
    int n;
    cin >> n;
    int count;
    if (pow(int(sqrt(n)), 2) == n) {
        count = 1;
    }
    for(int i=1, i<=)
    return 0;
}